<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.balaaca</groupId>
  <artifactId>balaaca-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Balaaca backend</name>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
    <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
    <quarkus.platform.version>3.39.1</quarkus.platform.version>
    <surefire-plugin.version>3.5.2</surefire-plugin.version>
    <failsafe-plugin.version>3.5.2</failsafe-plugin.version>
    <archunit.version>1.4.1</archunit.version>
    <assertj.version>3.27.7</assertj.version>
    <jqwik.version>1.10.1</jqwik.version>
    <jackson.version>2.22.1</jackson.version>
    <jacoco.version>0.8.15</jacoco.version>
    <pitest.version>1.30.0</pitest.version>
    <pitest-junit5.version>1.2.3</pitest-junit5.version>
  </properties>

  <modules>
    <!-- Framework-free. The domain imports this and nothing else. -->
    <module>shared-kernel</module>
    <!-- Framework-coupled cross-cutting machinery. No domain/ may depend on it. -->
    <module>platform-kernel</module>
    <module>identity</module>
    <module>providers</module>
    <module>catalog</module>
    <module>scheduling</module>
    <module>booking</module>
    <module>billing</module>
    <!-- The single deployable: migrations, the OpenAPI document, wiring. -->
    <module>app</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <!--
        Declared BEFORE the Quarkus BOM, which imports jackson-bom itself: on an
        import, the first declaration wins. Quarkus 3.39.1 pins 2.22.0, and two
        advisories against jackson-databind are fixed in 2.22.1 - a @JsonView
        bypass on unwrapped properties, and case-insensitive deserialization
        defeating a per-property @JsonIgnoreProperties. Both turn a field the
        server meant to refuse into one it accepts, which on a public booking
        payload is the wrong side to be wrong on.

        Remove this the day the platform ships 2.22.1 or later; until then it is
        a pin with a reason, not a floating override.
      -->
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.balaaca</groupId>
        <artifactId>shared-kernel</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.balaaca</groupId>
        <artifactId>platform-kernel</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>net.jqwik</groupId>
      <artifactId>jqwik</artifactId>
      <version>${jqwik.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.pitest</groupId>
          <artifactId>pitest-maven</artifactId>
          <version>${pitest.version}</version>
          <dependencies>
            <dependency>
              <groupId>org.pitest</groupId>
              <artifactId>pitest-junit5-plugin</artifactId>
              <version>${pitest-junit5.version}</version>
            </dependency>
          </dependencies>
        </plugin>
        <plugin>
          <groupId>io.smallrye</groupId>
          <artifactId>jandex-maven-plugin</artifactId>
          <version>3.6.0</version>
          <dependencies>
            <!-- The plugin pulls plexus-utils 3.5.1, which carries
                 GHSA-6fmv-xxpf-w3cw. Overriding it here fixes every module at
                 once; declaring the plugin eight times would mean eight places
                 to forget the next time. -->
            <dependency>
              <groupId>org.codehaus.plexus</groupId>
              <artifactId>plexus-utils</artifactId>
              <version>3.6.1</version>
            </dependency>
          </dependencies>
          <executions>
            <execution><id>make-index</id><goals><goal>jandex</goal></goals></execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!--
        Both agents, because both suites matter: a domain rule is covered by a
        unit test and an RLS policy only ever by an *IT, and a report that saw
        one of the two would understate the half it missed. They write to
        separate exec files and the report merges them.
      -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution>
            <id>unit-agent</id>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>integration-agent</id>
            <goals><goal>prepare-agent-integration</goal></goals>
          </execution>
          <execution>
            <id>merge</id>
            <phase>verify</phase>
            <goals><goal>merge</goal></goals>
            <configuration>
              <fileSets>
                <fileSet>
                  <directory>${project.build.directory}</directory>
                  <includes><include>jacoco*.exec</include></includes>
                </fileSet>
              </fileSets>
              <destFile>${project.build.directory}/jacoco-merged.exec</destFile>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
            <configuration>
              <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>${failsafe-plugin.version}</version>
        <!-- Container-backed tests are *IT and run at verify, so a plain
             `mvn test` stays fast and does not need a Docker daemon. -->
        <executions>
          <execution>
            <goals><goal>integration-test</goal><goal>verify</goal></goals>
            <configuration>
              <systemPropertyVariables>
                <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
              </systemPropertyVariables>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire-plugin.version}</version>
        <configuration>
          <systemPropertyVariables>
            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
